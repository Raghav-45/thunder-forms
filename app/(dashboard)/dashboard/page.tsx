import Layout from '@/containers/dashboard/layout'
import { FC } from 'react'

interface pageProps {
  
}

const page: FC<pageProps> = ({}) => {
  return (
    <Layout>
      <div>page</div>
    </Layout>
  )
}

export default page